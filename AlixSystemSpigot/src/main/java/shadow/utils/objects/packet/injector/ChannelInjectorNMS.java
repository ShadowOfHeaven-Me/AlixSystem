/*
package shadow.utils.objects.packet.injector;

import io.netty.channel.Channel;
import org.bukkit.Material;
import org.bukkit.entity.Player;
import shadow.utils.objects.packet.injector.nms_impl.NmsChannelGetter;

public final class ChannelInjectorNMS implements ChannelInjector {

    private final NmsChannelGetter impl = NmsChannelGetter.createImpl();

    @Override
    public Channel getChannel(Player p) throws Exception {
        return this.impl.getChannel(p);
    }

    @Override
    public String getProvider() {
        return "NMS-" + this.impl.getVersion();
    }
}*/
