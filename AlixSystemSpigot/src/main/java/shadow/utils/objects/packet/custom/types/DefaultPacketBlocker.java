/*
package shadow.utils.objects.filter.packet.custom.types;

import cf.zdybek.packethandler.handlers.InChat;
import cf.zdybek.packethandler.handlers.InClientCommand;
import cf.zdybek.packethandler.handlers.InKeepAlive;
import cf.zdybek.packethandler.handlers.OutKeepAlive;
import org.bukkit.entity.Player;
import shadow.utils.objects.filter.packet.custom.PacketBlocker;

public class DefaultPacketBlocker extends PacketBlocker {

    public DefaultPacketBlocker(Player p) {
        super(p);
    }

    @Override
    public boolean inClientCommand(InClientCommand inClientCommand) {
        return true;
    }

    @Override
    public boolean inChat(InChat inChat) {
        return true;
    }

    @Override
    public boolean inKeepAlive(InKeepAlive inKeepAlive) {
        return true;
    }
}*/
/**/
